---
description: "Pull latest changes and rebase"
---
Pull the latest changes from the remote and rebase the current branch on top.

Fetched refs:
!`git fetch --all`

Rebase command result:
!>`git rebase origin/main`

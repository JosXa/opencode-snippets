{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "opencode-snippets Configuration",
  "description": "Configuration file for the opencode-snippets plugin",
  "type": "object",
  "definitions": {
    "booleanSetting": {
      "oneOf": [{ "type": "boolean" }, { "type": "string", "enum": ["enabled", "disabled"] }],
      "description": "Boolean setting that accepts true, false, \"enabled\", or \"disabled\""
    }
  },
  "properties": {
    "$schema": {
      "type": "string",
      "description": "JSON Schema URL for editor autocompletion"
    },
    "logging": {
      "type": "object",
      "description": "Logging settings",
      "properties": {
        "debug": {
          "$ref": "#/definitions/booleanSetting",
          "default": false,
          "description": "Enable debug logging to file. Logs are written to ~/.config/opencode/logs/snippets/daily/"
        }
      },
      "additionalProperties": false
    },
    "experimental": {
      "type": "object",
      "description": "Experimental features (may change or be removed in future versions)",
      "properties": {
        "skillRendering": {
          "$ref": "#/definitions/booleanSetting",
          "default": false,
          "description": "Enable skill rendering with <skill>name</skill> or <skill name=\"name\" /> syntax. When enabled, skill tags are replaced with the skill's content body. Skills are loaded from OpenCode's standard skill directories."
        },
        "injectBlocks": {
          "$ref": "#/definitions/booleanSetting",
          "default": false,
          "description": "Enable <inject>...</inject> blocks for persistent context messages. When enabled, inject blocks in snippets are sent as separate user messages that persist throughout the agentic loop."
        }
      },
      "additionalProperties": false
    },
    "hideCommandInOutput": {
      "$ref": "#/definitions/booleanSetting",
      "default": false,
      "description": "Hide the shell command in snippet output. When false (default), shell commands show as '$ command\\n--> output'. When true, only the output is shown (matching OpenCode's slash command behavior)."
    }
  },
  "additionalProperties": false
}

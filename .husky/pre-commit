# Run formatting and linting (fixes issues automatically)
bun run ai:check

# Re-stage any files that were auto-fixed
git add -u

# Fail on any remaining biome errors or warnings
bunx biome check --error-on-warnings .

# Type check
bun run build

# Run tests
bun test
